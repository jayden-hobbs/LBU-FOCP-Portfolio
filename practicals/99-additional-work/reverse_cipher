#!/usr/bin/env python3

message = input("Enter the message to be encrypted: ")
translated = ''

i = len(message) - 1
while i >= 0:
    translated = translated + message[i]
    i = i - 1

print(translated)